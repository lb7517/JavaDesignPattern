代理模式（Proxy Pattern）中，一个类代表另一个类的功能。这种类型的设计模式属于结构型模式。


注意:
    1. 与工程模式的区别是，工厂模式是创建产品对象的的，不对外提供产品功能； 代理模式是代理产品对象对，外提供产品的功能
    2. springboot2.x默认使用cglib代理，设置 spring.aop.proxy-target-class=false; 接口采用jdk动态代理，
普通类采用cglib代理